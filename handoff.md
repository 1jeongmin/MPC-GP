# handoff

세션이 끝나거나 `/clear` 하기 전에 이 파일을 갱신한다. 5개 섹션 구조를 유지하라
(오늘 한 일 3줄 / 다음에 먼저 할 일 / 절대 하지 말 것 / 미결 결정 / 참고 경로).

**과거 실험 로그는 분리돼 있다** (2026-07-30, 파일이 커져서 분리):
- `handoff-log-phase0-7.md` — Phase 0~7 산출물 메모, 확정 결정, 기준선 수치
- `handoff-log-phase8.md` — Phase 8/8b/8c 결과(★ 전부 옛 비용함수 기준, 무효)

이 파일은 **지금 세션에 필요한 현재 상태**만 담는다.

---

## ⚠️⚠️ 새 세션에서 가장 먼저 읽을 것 (2026-07-30)

`main` 이 최근 세 번 바뀌었다 — MPC 비용함수, 공통 상태추정기, 입력 블로킹.
**옛 config 로 돌린 결과는 전부 무효**로 취급하라.

### ★★★ racetrack 은 검증 전용이다 — GP 학습 금지 (2026-07-30, 정책 확정)

`configs/gp/racetrack.yaml`이 `train_experiment: gp_train_racetrack`(경로 = racetrack
자체)를 가리켜 "racetrack 으로 학습 → racetrack 에서 평가(in-distribution)" 구조였다.
**이건 금지된 패턴이다.** 사용자 지시로 그 경로를 완전히 폐기했다:

- `configs/gp/racetrack.yaml`, `configs/experiment/gp_train_racetrack.yaml` **삭제**.
- `part1_gp_rt_ay4/ay6/rturn`, `part1_gp_rtn_*`, `part1_gp_rtf_*`(9개, `path: racetrack`
  또는 `right_turn`은 그대로) 전부 `gp: racetrack` → **`gp: loop10`**으로 재연결.
  이제 이 9개 시나리오는 "racetrack 에서 in-distribution 평가"가 아니라
  **loop10 학습 GP 의 형상외삽(OOD) 검증**이다 — 논문 서술을 그렇게 고쳐라.
- racetrack 으로 학습된 GP 로 만들어진 옛 평가 결과 16개 폴더는
  `results/results_729_deprecated_racetrack_gp/`로 옮겼다(삭제는 안 함, 참고용
  보관 — README.txt 참조). **논문/보고서에 쓰지 마라.**
- GP 학습 출처는 이제 **loop10 하나뿐**이다 (`configs/gp/loop10.yaml` ->
  `gp_train_loop10` -> `configs/sim/gp_train_loop10.yaml`, 10바퀴).

이 결정을 뒤집으려면(즉 racetrack 을 다시 학습에 쓰려면) 반드시 사용자에게 먼저 물어라.

### loop10 폐루프 "N바퀴" 기능 신설 (2026-07-30)

10바퀴 학습이 원래 코드로는 **불가능했다** — `Reference.kappa_of_s`가 s 를
`[0, total_length]`로 clamp 하고 `run_closed_loop`도 1바퀴에서 즉시 종료했다.
아래를 추가해서 해결했다 (기본값에서는 기존 모든 경로 동작이 그대로 유지된다 —
회귀 없음, 73개 테스트 통과):

- `PathConfig.closed_loop: bool = False` (`src/config.py`) — loop10 만 `true`.
- `SimConfig.n_laps: int = 1` (`src/config.py`) — `gp_train_loop10` 만 `10`.
- `Reference.kappa_of_s`: `closed_loop`면 `np.mod(s, total_length)`로 wrap
  (`src/path/reference.py`). `vx_max_of_s`/`get_preview`는 이 함수를 통해서만
  s 를 다뤄서 자동 전파된다.
- `run_closed_loop` 종료 조건: `s > total_length * n_laps + stop_margin`
  (`src/sim/runner.py`). `n_laps>1`인데 `closed_loop=False`면 예외.

이 기능은 loop10 전용이 아니라 범용이다 — 나중에 다른 폐루프 경로도
`closed_loop: true`만 켜면 N바퀴를 돌릴 수 있다.

### 현재 MPC 정식화 (요지만 — 전체는 `.claude/rules/mpc-solver.md`)

```
비용: Σ[ 10·e_y² + 5·e_psi² + 0·v_y² + 0.1·(gamma-vx·kappa)² + 0·delta² + 10·(Δdelta)² ]
지평: N=50, 입력 블로킹 [1]x20+[4]x5+[10]x1 (자유 20 + 꼬리 굵은 블록)
솔버: IPOPT + warm_start_init_point 등 4옵션 (solve time -22~48%, 정확도 무손실)
```

`v_y`·`delta` 크기 페널티 제거, `gamma` 만 참조 상대화 — 근거·측정은
`mpc-solver.md`「크기 페널티를 쓸 수 있는 양」「지평을 늘릴 때」「솔버 운영」에
전부 있다. **이 파일에 재설명하지 않는다.**

### 현재 피드백 파이프라인

```
플랜트 -> [센서 잡음] -> [공통 StateKF] -> x_hat -> MPC   (세 케이스 공통)
```

`configs/sensor/realistic.yaml` + `configs/state_kf/default.yaml`.
`src/estimation/state_estimator.py` 가 **상태추정기**(x_hat 만 만듦),
`AugmentedKF`(`ekf.py`)는 **모델보정자**(d_hat 만 예측모델에 주입) — 역할이 다르다,
섞지 마라(`ekf-baseline.md`).

**필터링이 이제 기본 구성이다.** 생측정 피드백(`rtn_*`)은 **성립하지 않는다**
(아래 참조) — ablation 으로만 쓴다.

### 무엇이 무효인가

| 대상 | 상태 |
|---|---|
| `handoff-log-phase8.md` 의 모든 결과표 | **무효** — 옛 비용함수 |
| README/RESULTS.md 의 모든 표 | **무효** — 옛 비용함수 |
| `configs/ekf/*.yaml` 의 `Q_d`(모델보정 KF) | **미검증** — 폐루프 튜닝값, `rtn_*` 에서 발산 확인됨 |
| `configs/state_kf/default.yaml` 의 `Q`(상태추정기) | **재검증 완료** — 아래 참조 |
| `configs/gp/*.yaml` 의 `sigma_n_floor`, `M` | **무효 가능** (미검증) |
| GP 학습 데이터셋 | 분포 이동(매 런 재학습이라 자기정합적) |
| racetrack 학습 GP 로 만든 모든 `rt_*/rtn_*/rtf_*` **MPC+GP** 결과 | **무효·폐기** — racetrack 학습 금지(위 ★★★), `results/results_729_deprecated_racetrack_gp/`로 이동 |
| `handoff-log-phase0-7.md` 의 측정값(기준선)·Phase 5 특성화 | **유효** — 컨트롤러 무관 |

**옛 수치와 새 수치를 같은 표에 섞지 마라.** 목적함수가 다르면 다른 실험이다.

### ★★ `rtn_*`(잡음 + 생측정, 필터 없음)이 성립하지 않는다

측정됨(원래 `results/results_729/part1_20260730_144450/`, 지금은
`results/results_729_deprecated_racetrack_gp/`로 이동됨 — 아래 참조,
`rtn_ay4/ay6/rturn` 9런). **`mpc_only`/`MPC+KF` 열은 GP 와 무관해 여전히 유효하다.
`MPC+GP` 열은 그 GP 가 racetrack 학습본(폐기됨)이라 무효 — loop10 학습본으로
재측정 필요:**

| 시나리오 | mpc_only | MPC+KF | MPC+GP (★무효, racetrack 학습) |
|---|---|---|---|
| rtn_ay4 | 1.6998e-2 | **32.6 m 발산** | ~~1.7757e-2 (+4.5%, mpc_only 보다 나쁨)~~ |
| rtn_ay6 | 3.5348e-2 | **3534 m 발산** | ~~2.9487e-2 (−16.6%)~~ |
| rtn_rturn | 1.5883e-2 | 1.3997e-2 (−11.9%) | ~~1.4145e-2 (−10.9%)~~ |

`dt_ctrl` 초과율 21.5~55.9%, 조향 rate 포화 ≈50%. 원인: 모델보정 KF 의 `Q_d`
(scale 0.1)가 옛 비용함수 아래 폐루프 튜닝된 값이라 비용함수가 바뀌자 같은
발산이 되돌아옴(상세: `handoff-log-phase8.md`「KF 초기 발산」). **폐루프 튜닝된
파라미터는 비용함수에 종속** — 비용함수를 바꾸면 재튜닝 대상에 올려라.

### ★ 공통 상태추정기(축 A) 재검증 — 새 정식화에서도 유효

loop10 + `sensor:realistic`, mpc_only, 비선형 플랜트:

| 설정 | 정규화 추정 RMSE 합 | RMS e_y | rate 포화 |
|---|---|---|---|
| 필터 없음 (생측정) | 3.979 (채널당 ≈1.0) | 1.5736e-2 | **52.5%** |
| **필터, Q 상속값 (x1.0)** | **1.911** | **1.3691e-2** | **0.0%** |
| 필터, Q x0.3 | 1.825 | 1.5940e-2 | 0.0% |
| 필터, Q x3.0 | 2.105 | 1.2947e-2 | 0.0% |

필터가 rate 포화를 52.5%→0%로 없앤다. 상속된 `state_kf.Q` 는 자기 기준(추정
RMSE)의 최적 근처에 여전히 있어 재튜닝이 급하지 않다. **추정 RMSE 와 추종오차가
반대 방향을 가리킨다**(Q 내리면 추정↑추종↓, 올리면 반대) — `state_kf` 는 추정
RMSE 기준으로 튜닝하기로 이미 정해져 있다(미결 #4). **뒤집으려면 먼저 물어라.**

### `feature/state-estimator-feedback` 브랜치는 완전히 흡수됐다

축 B(비용함수, `f8b3f5b`/`0c5badd`)와 축 A(상태추정기, `cdbdb47`)를 따로
cherry-pick 했다. 브랜치에 `main` 에 없는 코드는 이제 없다.

---

## 오늘 한 일 (3줄)

- 2026-07-30 — MPC 비용함수 수정(축 B) + 공통 상태추정기 재도입(축 A) + 입력
  블로킹(N=50, 자유20+꼬리굵은블록) + IPOPT warm start. 전부 `main` 병합, 테스트
  73개 통과. loop10 맵 신설(10코너 폐루프, 2277m, racetrack 2.38배).
- **드러난 것**: 기존 GP 우위는 두 보정기가 손댈 수 없는 컨트롤러 자체 편차
  (~6e-3)가 깔린 상태에서 측정된 것이었다. 그 편차를 없애자 GP·KF 격차가 2.1%로
  압축되며 순위가 뒤집혔다 — `test_gp.py` 의 `e_gp<e_kf` 게이트 제거(주 지표는
  캘리브레이션).
- **비대칭 확인**: 깨끗한 조건에서는 비용함수 수정이 전 지표 개선이지만 잡음
  조건(필터 없이)에서는 악화·KF 발산. 필터링을 기본 구성으로 확정해 해소.
- 문서 정리 — `handoff.md`/`prompts.md`/`README.md` 를 여러 파일로 분리
  (각 300줄 미만 유지 목적).
- **(이어서, 같은 날) racetrack GP 학습 금지 확정** — `gp: racetrack`(학습=평가
  트랙 자체) 패턴을 폐기하고, `Reference`/`SimConfig`에 폐루프 N바퀴 기능을
  새로 추가해 GP 학습을 **loop10 10바퀴로만** 고정했다. racetrack 평가 9개
  시나리오는 `gp: loop10`으로 재연결(이제 in-distribution 이 아니라 형상외삽
  검증). racetrack 학습 GP 로 만든 옛 결과 16개 폴더는
  `results/results_729_deprecated_racetrack_gp/`로 격리(삭제 아님, 참고용).
  상세는 위 「★★★ racetrack 은 검증 전용이다」참조. **10바퀴 학습 실행·검증
  완료** — 제어스텝 52665개, 최종 s=22774.7 m(목표 22770.0 m, +4.7 m, stop_margin
  5 m 이내 — 정확히 10.0021 바퀴), 최종 t=1053.3 s(duration 1150 s 상한 안쪽,
  조기절단 아님). 잔차 RMS: r_vy=1.071e-2 m/s, r_gamma=8.733e-4 rad/s. 학습된
  하이퍼파라미터: v_y lengthscales(표준화)=[2.80, 0.31, 1.49] sigma_f=1.11
  sigma_n=0.0100(floor 에 닿음), gamma lengthscales=[0.86, 0.0011, 0.0010]
  sigma_f=1.35 sigma_n=0.126. **참고**: gamma 채널의 delta/gamma 차원
  lengthscale 이 하한(1e-3) 근처다 — 그 차원 민감도가 매우 날카롭게 잡혔다는
  뜻인데 버그인지 데이터 특성인지 아직 판단하지 않았다(수동 조정 금지 원칙상
  그대로 둠 — 다음 세션에서 `casadi_export.py` 대조 테스트로 확인할 것).
  pytest 73개 재실행 통과. 전체 실행 로그:
  `train_loop10_gp.log`(세션 스크래치패드, 재현 스크립트 `train_loop10_gp.py`
  동봉 — 저장소 밖이라 다음 세션에서 못 열 수 있음, 필요하면 재실행해서 같은
  수치 재현 가능).

  **★ rt_ay4/rt_ay6 재실행 완료** (`results/part1_20260730_224024/`, loop10 학습
  GP, racetrack 평가 — 이제 진짜 형상외삽/OOD). `rt_rturn`/`rtn_*`/`rtf_*` 6개는
  아직 안 돌렸다(다음 할 일 유지).

  | 시나리오 | 케이스 | RMS e_y | z_std | 95% 커버 | NLPD |
  |---|---|---|---|---|---|
  | rt_ay4 | mpc_only | 9.325e-3 | - | - | - |
  | rt_ay4 | kf | 1.956e-3 | 0.565 | 99.6% | -5.05 |
  | rt_ay4 | gp | 2.812e-3 | 2.527 | 74.6% | -3.65 |
  | rt_ay6 | mpc_only | 2.772e-2 | - | - | - |
  | rt_ay6 | kf | 4.039e-3 | 1.463 | 89.6% | -4.13 |
  | rt_ay6 | gp | 2.072e-2 | 4.636 | 44.3% | **+5.42** |

  **이게 이 실험 축의 진짜 의미다** — 예전 rt_* 결과(폐기됨)는 GP가 racetrack
  자체로 학습돼 in-distribution 이라 캘리브레이션이 좋아 보였다. loop10 학습으로
  바꾸고 나니(진짜 OOD) GP 커버리지가 rt_ay4 에서 74.6%(목표 95%), rt_ay6 에서
  44.3%까지 무너지고 NLPD 도 rt_ay6 에서 양수로 뒤집힌다 — **과신이 뚜렷해짐**.
  추종오차(RMS e_y) 자체는 GP 가 여전히 mpc_only 보다 낫지만 KF 보다는 못하다.
  이건 버그가 아니라 정직한 OOD 평가의 기대 결과에 가깝다(gp-residual.md 의
  "저마찰 구간 없이 학습 -> 슬립 구간 평가는 의도된 실험"과 같은 원리) — 다만
  이 결과를 논문에 어떻게 서술할지는 미결 #2(GP 과신 서술 범위)와 합쳐서
  다시 논의해야 한다.

  **★ rtf_ay4/rtf_ay6 도 재실행 완료** (`results/part1_20260730_225803/`, 잡음 +
  공통 KF 필터). rt_*(잡음 없음) 대비 전 케이스 캘리브레이션이 크게 나빠진다 —
  **KF 도** rtf_ay6 에서 z_std=5.35, 95%커버 51.3%, NLPD=+8.49 로 심하게 과신한다
  (rt_ay6 에서는 KF 가 z_std=1.463 로 훨씬 나았다). GP 는 rtf_ay6 에서 z_std=4.76,
  95%커버 36.0%, NLPD=+6.15. 즉 잡음+필터 조건에서는 **GP 뿐 아니라 KF 도
  과신한다** — `state_kf.Q`(공통 상태추정기, 추정 RMSE 기준 튜닝, 미결 #4)가
  이 캘리브레이션까지 커버하도록 설계된 게 아니라서 나온 결과로 보이나
  원인 판별은 안 했다(다음 세션 후보 작업).

  | 시나리오 | 케이스 | RMS e_y | z_std | 95% 커버 | NLPD |
  |---|---|---|---|---|---|
  | rtf_ay4 | mpc_only | 1.873e-2 | - | - | - |
  | rtf_ay4 | kf | 1.152e-2 | 2.209 | 73.5% | -3.39 |
  | rtf_ay4 | gp | 1.230e-2 | 2.577 | 69.2% | -3.13 |
  | rtf_ay6 | mpc_only | 4.408e-2 | - | - | - |
  | rtf_ay6 | kf | 2.153e-2 | 5.351 | 51.3% | **+8.49** |
  | rtf_ay6 | gp | 3.748e-2 | 4.763 | 36.0% | **+6.15** |

  rt_rturn/rtn_*/rtf_rturn(형상외삽 축, 6개)은 사용자 판단으로 **생략 확정**
  (racetrack 자체가 이미 형상외삽이라 rturn 별도 축은 불필요하다는 판단).

### ★★★ KF 재튜닝 완료 — 과신의 원인은 모델보정 KF 였다 (2026-07-30)

**튜닝 기준 (사용자 승인)**: 점추정치 정확도 = 추정/외란 RMSE(미결 #4 준수),
공분산 `P` 크기 = **혁신일관성 NIS**(E[NIS]=n_meas=4) 로 **병행** 판단. 캘리브레이션
(z_std/커버리지)은 **맞추는 대상이 아니라 결과로만** 본다. 하네스는
`scripts/tune_kf.py`(재현 가능 — 옛 튜닝은 스크립트가 없어 재현 불가였다),
결과 원본은 `results/tuning/tune_{state_kf,ekf}_*.json`.

**NIS 인프라 신설**: `AugmentedKF.update()` 가 `S` 로 `self.nis` 저장(`ekf.py`),
runner 가 `ekf_nis`/`skf_nis` 로깅. 정합 필터에서 평균 NIS=3.991(이론 4.0),
중앙값 3.327(chi2_4 이론 3.357)로 검증(`tests/test_ekf.py` 신규 2개).

#### ① 공통 상태추정기(`state_kf`) — **변경 없음. 범인이 아니었다.**

rtf_ay4 에서 현재값 대비 7점 재그리드(주 지표 = 정규화 추정 RMSE):
`x0.1 -> 2.8404 | x0.3 -> 2.0896 | **x1.0(현재) -> 2.0308 최적** | x3 -> 2.1944 |
x10~100 -> 2.33~2.41`. **현재값이 이미 최적**이라 그대로 뒀다.

**결정적 진단**: 현재값의 NIS=3.15 로 4 보다 **작다** = P 가 오히려 약간 크다
(보수적). 과신이면 NIS 가 4 보다 훨씬 커야 하는데 반대다. 즉 rtf_* 의 과신은
이 필터에서 온 것이 아니다. NIS 최적은 x0.3(4.26)이었으나 주 지표(미결 #4 확정
기준)가 x1.0 을 가리키고 보수적 방향이 UQ 에서 안전하므로 x1.0 유지로 사용자 확인.

#### ② 모델보정 KF(`configs/ekf/realistic_sensor.yaml`) — **범인. Q_d x10 으로 교정.**

**원인 규명**: Phase 8c 의 Q_d 튜닝이 **추종오차(RMS e_y)** 그리드였다 — 미결 #4
("목적함수는 추정/외란 RMSE 이지 추종오차가 아니다", **확정**) 위반이다. 추종오차는
세 케이스가 공유하는 MPC·경로에 지배돼 필터 품질을 거의 반영하지 못한다(당시 8점
전체가 1.3% 안에 몰린 것이 증거). 그렇게 고른 scale 0.1 이 **Q_d 를 10배 낮춰
P_d 를 과소하게** 만든 것이 과신의 직접 원인이었다.

올바른 목적함수(정규화 외란 RMSE)로 재그리드 — **주 지표와 NIS 가 같은 지점 일치**:
`x0.1 -> 1.2904(NIS 8.56) | x0.3 -> 1.1736(6.77) | x1.0 -> 1.0355(5.39) |
x3 -> 0.9299(4.61) | **x10 -> 0.8867(4.09) 두 기준 모두 최적** | x30 -> 0.9515(3.77) |
x100 -> 1.1563(3.47)`.
`Q_kf_diag` d-블록 `[1.0e-1, 1.0e-2] -> [1.0, 1.0e-1]` — 이는 **`ekf/default.yaml`
과 같은 값**이다. 잘못된 튜닝이 낮춘 것을 원위치시킨 셈이다.

#### 전/후 비교 (★ rtf_ay6 은 튜닝에 쓰지 않은 **held-out**)

| 시나리오 | 케이스 | RMS e_y 전→후 | z_std 전→후 | 95%커버 전→후 | NLPD 전→후 |
|---|---|---|---|---|---|
| rtf_ay4 | mpc_only | 1.873e-2 → 1.873e-2 | - | - | - |
| rtf_ay4 | **kf** | 1.152e-2 → **1.147e-2** | 2.209 → **0.931** | 73.5% → **94.6%** | −3.39 → **−4.56** |
| rtf_ay4 | gp | 1.230e-2 → 1.230e-2 | 2.577 → 2.577 | 69.2% → 69.2% | −3.13 → −3.13 |
| **rtf_ay6** | mpc_only | 4.408e-2 → 4.408e-2 | - | - | - |
| **rtf_ay6** | **kf** | 2.153e-2 → **2.033e-2** | 5.351 → **1.786** | 51.3% → **79.6%** | **+8.49 → −3.39** |
| **rtf_ay6** | gp | 3.748e-2 → 3.748e-2 | 4.763 → 4.763 | 36.0% → 36.0% | +6.15 → +6.15 |

mpc_only/gp 가 전후 동일한 것은 **정상**이다(둘 다 모델보정 KF 를 쓰지 않는다) —
통제가 제대로 됐다는 증거로 읽어라. pytest 75개(기존 73 + NIS 2) 통과.

#### ★ 정직한 한계 — 다 고쳐진 게 아니다

1. **held-out rtf_ay6 은 여전히 과신이다**: z_std=1.786(>1), 95%커버 79.6%(<95%).
   크게 줄었을 뿐 해소된 게 아니다. a_y=4 에서 튜닝한 Q_d 로 a_y=6 의 커진 모델오차를
   다 덮지 못하는 것은 구조적으로 당연하다(고정 Q 의 한계 — `ekf-baseline.md` 가
   말하는 "aleatoric 을 시간축으로 전파할 뿐 공간적으로 분해하지 못한다").
2. **채널 불균형**: rtf_ay6 에서 v_y 는 z_std=1.045/92.3% 로 거의 맞는데 gamma 가
   z_std=2.299/66.9% 로 나쁘다. 채널별 Q_d 비율(현재 10:1)이 최적이 아닐 수 있다
   — 이번엔 두 채널을 **같은 배수**로만 스케일했다(미결 #14).
3. **★ 논증에 불리한 결과**: 이제 KF 가 GP 를 **캘리브레이션·추종 양쪽에서 앞선다**
   (rtf_ay6: KF z_std 1.786/커버 79.6%/NLPD −3.39 vs GP 4.763/36.0%/+6.15,
   추종도 2.03e-2 vs 3.75e-2). 비교군을 정당하게 강화한 결과이므로 되돌리면 안 된다
   (`ekf-baseline.md` 「strawman 비교군은 논문을 죽인다」). **GP 우위 서사를 그대로
   쓸 수 없게 됐다** — 미결 #2·#3(GP 과신 서술 범위)와 묶어 다시 판단해야 한다.

### ★★★ GP 과신의 진짜 원인 규명 — 분포이동이 아니라 M=100 부분표본 (2026-07-31)

**사용자 가설**: "GP 가 참값으로 학습되는데 배포에서는 KF 로 거른 값을 받는다."
가설은 타당했고 실제 불일치였다. 고쳤다:

- `collect_residual_data`(`src/gp/dataset.py`)가 `sensor`/`state_estimator` 를 받고,
  특징을 `log["x"]`(참값) -> **`log["x_fb"]`**(제어기가 실제로 받는 상태)에서 뽑는다.
  센서가 없으면 `x_fb == x` 이므로 **기존 경로는 결과가 동일**(테스트로 확인).
- `configs/experiment/gp_train_loop10.yaml` 에 `sensor: realistic` + `state_kf: default`.
- `train_gp_from_config`(`assemble.py`)가 `run_case` 와 **같은 팩토리**로 주입.
- **타깃(잔차)은 참값 기준 그대로 유지** — 상태추정 오차 전파는 상태추정기 책임이고
  (`ekf-baseline.md` 역할 혼동 금지), 타깃을 바꾸면 캘리브레이션 비교 기준이 달라져
  KF 수치까지 재정의된다.

**실제로 좋아진 것** (학습 품질 자체):
- gamma 채널 lengthscale 이 하한(1e-3) 접촉에서 벗어남: `[0.86, 0.001, 0.001]`
  -> `[0.447, 0.451, 0.271]`. 2026-07-30 에 "버그인지 데이터 특성인지 미판단"으로
  남겼던 퇴화가 **해소**됐다.
- `sigma_n` 상승: v_y `0.0100`(floor 접촉) -> `0.0288`(floor 이탈).
- 추종오차도 소폭 개선(rtf_ay4 1.230e-2 -> 1.211e-2, rtf_ay6 3.748e-2 -> 3.568e-2).

**그러나 과신은 안 고쳐졌다. 오히려 rtf_ay4 는 나빠졌다** (아래 표).

#### ★ 결정적 진단 — in-distribution 에서조차 과신한다

케이스 1(`lpf_ay4`)은 GP 의 **학습맵·같은 a_y·같은 seed** 다. 그런데도:

```
lpf_ay4 GP:  z_std = 3.592,  95% 커버 = 63.3%,  z_mean = -0.085
```

`z_mean ≈ 0` 이므로 **평균은 잘 맞는다**. 실제로 GP 평균이 잔차의 89% 를 설명한다
(v_y: 잔차 RMS 1.079e-2, GP평균 RMS 1.088e-2, |r−mu| RMS 1.217e-3).
문제는 **분산뿐**이다 — 예측 sigma 3.43e-4 vs 실제 오차 1.217e-3 (**3.5배 과소**).

**즉 분포이동은 원인이 아니다.** 학습 분포 한가운데서도 3.5배 과신한다.

#### 원인: 학습이 M=100 부분표본에만 이뤄진다

`train()`(`train_offline.py`)은 `dataset.subsample(cfg.M)` 로 **52,665 스텝 중 100개**만
남기고 그 100개로 exact GP + Type-II ML 을 돌린다. RBF 가 100점을 거의 보간해버리므로
`sigma_n` 은 "그 100점 사이의 잔여 산포"만 설명하게 되어, 임의의 `z` 에서의 실제
조건부 산포보다 훨씬 작게 추정된다. 표준화 공간에서 `sigma_f/sigma_n` 이 v_y 36배,
gamma 27배인 것이 그 징후다(사전 대비 잡음이 지나치게 작다).

**이것이 미결 #2 의 실체다.** 이전에 "사후분산은 사전분산을 넘을 수 없다"(외삽 한계)로
정리했던 것과 별개로, **in-distribution 과신은 부분표본 학습에서 온다.**

**금지 사항 재확인**: 커버리지를 맞추려고 `sigma_n_floor`·분산을 사후 스케일링하지
마라. 고치려면 **학습 절차**를 고쳐야 한다(아래 선택지). 사용자 확인 없이 진행 금지.

선택지: (i) `M` 을 키운다(계산량 M^3), (ii) `sigma_n` 을 딕셔너리가 아니라 **홀드아웃
전체 데이터**로 추정, (iii) **제대로 된 sparse GP**(FITC/VFE — N개 전부를 쓰고 M개
inducing point 로 근사)로 전환 = 원래 Phase 9 계획, (iv) 한계로 정직히 서술.

#### 4케이스 결과 (전부 잡음 + 공통 상태추정기, GP 학습 = loop10 10바퀴 잡음+필터)

| # | 시나리오 | 케이스 | RMS e_y | z_std | 95%커버 | NLPD |
|---|---|---|---|---|---|---|
| 1 | lpf_ay4 (학습맵·a_y그대로, **in-dist**) | mpc_only | 1.3691e-2 | - | - | - |
| 1 | | **kf** | **8.6269e-3** | **0.831** | **96.1%** | **−4.64** |
| 1 | | gp | 8.8971e-3 | 3.592 | 63.3% | −1.07 |
| 2 | lpf_ay6 (학습맵·a_y증가) | mpc_only | 2.7550e-2 | - | - | - |
| 2 | | **kf** | **1.4548e-2** | **1.225** | **90.6%** | **−4.23** |
| 2 | | gp | 2.2543e-2 | 3.505 | 61.5% | −0.64 |
| 3 | rtf_ay4 (racetrack·a_y그대로) | mpc_only | 1.8725e-2 | - | - | - |
| 3 | | **kf** | **1.1473e-2** | **0.931** | **94.6%** | **−4.56** |
| 3 | | gp | 1.2108e-2 | 3.628 | 61.0% | −0.38 |
| 4 | rtf_ay6 (racetrack·a_y증가) | mpc_only | 4.4078e-2 | - | - | - |
| 4 | | **kf** | **2.0330e-2** | **1.786** | **79.6%** | **−3.39** |
| 4 | | gp | 3.5679e-2 | 4.098 | 52.5% | +3.48 |

**★ 케이스 1 은 in-distribution 진단이지 일반화 성능이 아니다** — 맵·a_y·seed 가
학습과 같다(학습은 10바퀴 mpc_only, 평가는 1바퀴 mpc_gp 라 궤적은 다르지만 분포는
겹친다). "GP 가 일반화한다" 의 근거로 쓰지 마라. 일반화 축은 케이스 2~4 다.

#### GP 학습조건 변경 전/후 (rtf_* 만 비교 가능 — lpf_* 는 신규라 이전 값 없음)

| 시나리오 | 지표 | 참값 학습(전) | 잡음+필터 학습(후) | 판정 |
|---|---|---|---|---|
| rtf_ay4 gp | z_std | 2.577 | 3.628 | **악화** |
| rtf_ay4 gp | 95%커버 | 69.2% | 61.0% | **악화** |
| rtf_ay4 gp | NLPD | −3.13 | −0.38 | **악화** |
| rtf_ay4 gp | RMS e_y | 1.2299e-2 | 1.2108e-2 | 소폭 개선 |
| rtf_ay6 gp | z_std | 4.763 | 4.098 | 개선 |
| rtf_ay6 gp | 95%커버 | 36.0% | 52.5% | 개선 |
| rtf_ay6 gp | NLPD | +6.15 | +3.48 | 개선 |
| rtf_ay6 gp | RMS e_y | 3.7478e-2 | 3.5679e-2 | 개선 |

**혼재다. 포장하지 않는다.** 추종은 둘 다 개선, 캘리브레이션은 ay4 악화·ay6 개선.
(추정) ay4 가 나빠진 것은 새 학습 주행이 잡음 피드백 탓에 조향이 더 활발해져
gamma 잔차 스케일이 2배가 됐기 때문일 수 있다(`r_scale` gamma 5.7e-4 -> 1.15e-3).
**미검증 추측이다.**

#### 되돌릴 것인가

되돌리지 **않는다.** 이유: (a) train/deploy 불일치는 실재했고 고치는 게 맞다,
(b) 퇴화 lengthscale 이 해소돼 학습 품질 자체가 나아졌다, (c) 추종오차는 전 케이스
개선, (d) 진짜 원인(M=100 부분표본)이 드러난 것이 이번 실험의 최대 소득이다.
다만 **캘리브레이션 개선은 이 변경으로 달성되지 않았다**는 사실을 그대로 기록한다.

### ★★★ GP 분산 공식 버그 수정 + M=100 -> 2000 (2026-07-31)

#### 발견: `predict_var` 가 **잠재분산 Var[f*]** 만 주고 관측잡음을 빼먹었다

M 을 키우면 좋아질 거라 예상하고 스윕했더니 **정반대**가 나왔다(M↑ -> NLPD↑ z_std↑).
원인은 M 이 아니라 분산 공식이었다. `var_std` 가 `sigma_f^2 − k^T W k` 를 반환하는데
이건 잠재함수 분산이고, 캘리브레이션이 비교하는 대상은 **관측된 잔차**이므로
`Var[y*] = Var[f*] + sigma_n^2` 를 써야 한다 (R&W 2006, Eq. 2.19 vs 2.24).

M 이 커질수록 `Var[f*]` 가 데이터 근처에서 0 으로 붕괴해 누락 효과가 증폭된다 —
M=1000 에서 v_y 의 `Var[f*]=2.35e-8` vs `sigma_n^2=3.66e-7` 로 **누락 항이 15배**였다.
그래서 추세가 뒤집혀 보였다.

**해결 (사용자 확정): 두 분산을 구분해 쓴다.**
- `predict_var` = `Var[f*]` — **불확실성 지도**(epistemic, "여기 데이터가 있었나")
- `predict_var_obs` = `Var[f*] + sigma_n^2` — **캘리브레이션**(관측을 덮는가)
- 둘 다 로깅(`gp_var`, `gp_var_obs`). `run_sim.compute_calibration` 은 후자를 쓴다
  (옛 로그는 `gp_var` 로 폴백 — 하위호환).

하나로 뭉치면 둘 중 한 질문에 반드시 틀린 답을 준다. KF 쪽은 이미 관측 공분산
개념이라 이 수정으로 **비교가 오히려 공정해졌다.**

#### M = 100 -> 2000 (홀드아웃 NLPD 기준, `scripts/tune_gp_M.py`)

보정 후 추세가 정상화됐다(홀드아웃 = 딕셔너리 밖 4000점):

|    M | 학습(s) |   NLPD | z_std | 95%커버 |
|-----:|--------:|-------:|------:|--------:|
|  100 |     0.3 | −5.489 | 1.800 |  87.6% |
|  250 |     1.9 | −5.980 | 1.452 |  91.3% |
|  500 |     5.3 | −6.146 | 1.350 |  91.5% |
| 1000 |    34.8 | −6.184 | 1.324 |  93.3% |
|**2000**| 201.6 |**−6.206**|**1.241**|**94.1%**|

**M=4000 은 채택 안 함**: 학습만 40분(실측), 스텝당 분산이 O(M^2)라 ~14 ms 로
`dt_ctrl=20 ms` 를 넘는다(M=2000 은 분산 3.6 ms + mu 0.8 ms 로 예산 안). NLPD 도 평평.
선택 기준을 **폐루프 추종오차가 아니라 홀드아웃 NLPD** 로 둔 이유: 추종오차로 고르면
미결 #4(목적함수는 추정 품질이지 추종오차가 아니다)를 GP 쪽에서 되풀이한다.

부수 개선: `var_std` 를 BLAS 형태(`sum((k@W)*k)`)로 교체 — `einsum` 과 수학적으로
동일하지만 M 이 커질 때 훨씬 빠르다.

#### 4케이스 결과 (M=2000 + 분산 수정, `results/part1_20260731_163230/`)

| # | 시나리오 | 케이스 | RMS e_y | z_std | 95%커버 | NLPD |
|---|---|---|---|---|---|---|
| 1 | lpf_ay4 (**in-dist**) | mpc_only | 1.3691e-2 | - | - | - |
| 1 | | **kf** | **8.6269e-3** | **0.831** | **96.1%** | **−4.64** |
| 1 | | gp | 8.9134e-3 | 2.192 | 80.6% | −4.55 |
| 2 | lpf_ay6 | mpc_only | 2.7550e-2 | - | - | - |
| 2 | | **kf** | **1.4548e-2** | **1.225** | **90.6%** | **−4.23** |
| 2 | | gp | 2.1688e-2 | 2.306 | 75.1% | −3.65 |
| 3 | rtf_ay4 | mpc_only | 1.8725e-2 | - | - | - |
| 3 | | **kf** | **1.1473e-2** | **0.931** | **94.6%** | **−4.56** |
| 3 | | gp | 1.2221e-2 | 2.609 | 76.3% | −3.11 |
| 4 | rtf_ay6 | mpc_only | 4.4078e-2 | - | - | - |
| 4 | | **kf** | **2.0330e-2** | **1.786** | **79.6%** | **−3.39** |
| 4 | | gp | 3.2710e-2 | 2.839 | 62.9% | −1.11 |

#### 전/후 (GP 케이스만. ★ 분산 수정 + M 확대가 **섞인** 효과다 — 분리 안 됐다)

| 시나리오 | z_std | 95%커버 | NLPD | RMS e_y |
|---|---|---|---|---|
| lpf_ay4 | 3.592 -> **2.192** | 63.3% -> **80.6%** | −1.07 -> **−4.55** | 8.897e-3 -> 8.913e-3 |
| lpf_ay6 | 3.505 -> **2.306** | 61.5% -> **75.1%** | −0.64 -> **−3.65** | 2.254e-2 -> **2.169e-2** |
| rtf_ay4 | 3.628 -> **2.609** | 61.0% -> **76.3%** | −0.38 -> **−3.11** | 1.211e-2 -> 1.222e-2 |
| rtf_ay6 | 4.098 -> **2.839** | 52.5% -> **62.9%** | +3.48 -> **−1.11** | 3.568e-2 -> **3.271e-2** |

전 케이스 캘리브레이션 대폭 개선, NLPD 는 rtf_ay6 에서 양수->음수로 반전. 추종은 보합.

#### ★ 그래도 목표 미달이다 — 포장하지 않는다

**in-distribution(lpf_ay4)에서조차 z_std=2.192, 95%커버 80.6%** 다. 목표(1.0, 95%)에
못 미치고 **KF(0.831, 96.1%)에 여전히 진다.**

홀드아웃에서는 같은 GP 가 z_std=1.241 / 94.1% 였는데 폐루프에서 2.192 / 80.6% 로
나빠지는 **격차가 핵심 미해결 문제**다. 원인 가설(미검증):
1. 홀드아웃 점은 **학습 궤적 위**(mpc_only 10바퀴)이고, 폐루프 평가는 **mpc_gp 가
   만든 다른 궤적**이다. GP 보정이 들어가면 방문 상태분포가 학습 때와 달라진다
   — 일종의 폐루프 분포이동(자기 유발).
2. 지평 상수 주입이라 `mu_hat` 은 `z_now` 에서 한 번 평가되는데, 그 사이 상태가
   움직인 만큼의 오차가 분산에 반영되지 않는다.

**다음 세션은 이 격차(1.241 vs 2.192)의 원인 판별부터 시작하라.** 가설 1 은
"mpc_only 로 평가한 로그"와 "mpc_gp 로 평가한 로그"의 z 분포를 대조하면 갈린다.

#### `save_training_results` 버그 (냈다가 고침, 같은 날)

`results/part1_*/training_results/` 기록이 **실행하지 않은 시나리오**의 GP
(`part1_gp_ay4` -> `gp: default`, M=100)를 집었다. 전체 `SCENARIOS` 를 훑은 탓.
**실험 런 자체는 정상**(meta.json 확인: M=2000, gp_train_loop10)이었고 기록만 틀렸다.
이번 배치에서 실제로 돌린 시나리오만 보도록 고쳤고, 배치 안에 학습 출처가 섞이면
예외를 던지게 했다. `part1_20260731_163230/training_results/` 는 재생성 완료.

### GP 학습 디스크 캐싱 추가 (2026-07-30)

`train_gp_from_config`(`src/sim/assemble.py`)가 프로세스 메모리 캐시(`_GP_CACHE`)
만 쓰고 있어서, `run_part1.py`를 새로 실행할 때마다(=새 프로세스) 같은 학습을
반복했다 — loop10 10바퀴처럼 500초대 학습이 매번 반복돼 사용자가 문제 제기.

`data/gp_cache/`(git 제외)에 npz 로 저장하는 디스크 캐시를 추가했다
(`_gp_disk_cache_path` 가 학습 experiment 스냅샷 + `GpConfig` 전체를 sha256 해시해
파일명으로 씀 — 학습 config 가 하나라도 바뀌면 자동으로 새 해시=재학습, 오래된
캐시를 몰래 재사용할 위험 없음). 검증: 첫 호출 4.22 s(학습+저장) -> 새 프로세스
재호출 0.037 s(로드), ay4/ay6(같은 학습 출처) 는 캐시 공유·rt(다른 학습 출처)는
별도 해시로 확인. pytest 73개 재통과.

**이 캐시는 로컬 산출물이다** — `results/`, `data/` 와 같은 성격, git 커밋 금지
(이미 `.gitignore` 의 `data/` 규칙에 포함됨). 학습 config 를 바꿔가며 실험할 때
디스크에 npz 가 계속 쌓이는데, 필요 없어지면 그냥 지워도 된다(재실행하면
다시 만들어짐).

## 다음에 먼저 할 일

1. **★ 홀드아웃(z_std 1.241) vs 폐루프(2.192) 격차의 원인 판별** — 미결 #16.
   같은 GP 인데 평가 방식만 다르면 이만큼 벌어진다. 이걸 모르면 "GP 로 잔차를
   해결할 수 있다"는 주장의 범위를 정할 수 없다. 판별 실험: 같은 시나리오를
   **mpc_only 로 돌린 로그**의 z 분포와 **mpc_gp 로 돌린 로그**의 z 분포를 대조
   (가설: GP 보정이 방문 상태분포를 바꾸는 자기 유발 분포이동).
   `sigma_n_floor`·분산 사후 스케일링은 여전히 **금지**(분식).
2. 실험은 **4케이스 체계**로 재편됐다(2026-07-31 사용자 지정):
   1=`lpf_ay4`(학습맵·그대로) 2=`lpf_ay6`(학습맵·증가) 3=`rtf_ay4` 4=`rtf_ay6`.
   `rt_*`/`rtn_*`/`*_rturn` 은 **생략 확정** — racetrack 자체가 형상외삽이라 rturn
   별도 축이 불필요하고, 잡음+필터가 기본 구성이라 무잡음/무필터 축도 안 쓴다.
   재개하려면 사용자에게 다시 확인.
3. **미결 #15 — 논문 논증 재설계**. KF 가 GP 를 캘리브레이션·추종 **양쪽에서**
   앞선다(4케이스 전부). #2 의 결정과 묶어서 판단할 것. #2 를 (iii)sparse GP 로
   가면 GP 캘리브레이션이 달라질 수 있으므로 **#2 를 먼저 정하는 것이 순서다.**
4. 위 항목들이 끝나야 README/RESULTS.md 갱신 가능 (둘 다 여전히 무효 상태).

> ~~`configs/gp/loop10.yaml` 의 `tuning_selection_scenario` 채우기(미결 #13)~~ —
> **완료**. `part1_gp_lpf_ay4` 로 채워져 있다.

## 절대 하지 말 것

- 게이트 1 실패 상태에서 MPC 구현 착수.
- GP 보정을 연속 우변(`xdot`)에 더하기 — `dt_ctrl`배 틀린다. 이산 결합만.
- 4x4 전체 `A`에 Hurwitz 검사 걸기 — 원점 고유값 2개는 정상이다.
- 테스트 허용오차를 늘려서 게이트 통과시키기.
- `src/`의 스텁 파일 삭제 (룰 자동 로드 트리거용).
- `results/`, `data/` 커밋.
- **커버리지를 맞추려고 사후분산을 스케일링하거나 `sigma_n_floor` 를 만지기.**
  과신은 측정된 사실이고, 숫자를 맞추면 측정이 아니라 분식이다.
- **GP 평가가 나쁘다고 `gp_train.yaml` 의 경로·a_y 를 바꾸기** (학습 데이터 몰래 늘리기).
- dlc 의 KF·GP 열세를 튜닝으로 덮기 — 원인 판별(오라클 대조)이 먼저다.
- **`W_vy`, `R_delta` 를 "작은 정규화"라며 되살리기** — 되살리려면 `mpc-solver.md`
  의 3시나리오 표(추종·전륜슬립·rate 포화가 동시에 개선)를 반증하라.
- **`gamma` 의 참조 상대화를 `delta`/`v_y` 로 확장하기** — 그 참조는 GP 가 학습할
  대상인 선형모델(`Cf, Cr, m, Iz`)에서 나온다. 확장하면 잔차 출처 통제가 깨진다.
- **옛 수치와 새 수치를 같은 표에 섞기.** 목적함수가 다르면 다른 실험이다.
- **비용함수를 바꾼 뒤 `Q_d`·GP 하이퍼파라미터를 그대로 두기** — 폐루프로 튜닝된
  파라미터는 비용함수에 종속이다. 2026-07-30 에 이걸 놓쳐 KF 가 다시 발산했다.
- **균등 입력 블로킹(꼬리 동결)을 쓰기** — 측정으로 확인됨: 변수가 더 적은데도
  더 느리고 추종도 나쁘다(`mpc-solver.md`「지평을 늘릴 때」).
- **racetrack 을 GP 학습 출처로 쓰기** (`gp.train_experiment` 가 racetrack 경로를
  가리키게 하기) — racetrack 은 검증 전용으로 확정됐다(2026-07-30, 위 ★★★).
  `configs/gp/racetrack.yaml`/`gp_train_racetrack.yaml` 은 삭제됐다 — 되살리지 마라.

## 미결 결정

확정되면 해당 룰 파일에서 "(제안)" 표기를 지우고 여기서도 지운다.

| # | 항목 | 위치 | 상태 |
|---|---|---|---|
| 1 | **dlc 에서 KF·GP 가 MPC-only 보다 나쁘다** — 설계 오류인가 시나리오 특성인가 | `ekf-baseline.md` 공정성 5 | 미결(옛 비용함수 기준, 재측정 필요) |
| 2 | **GP 과신을 고칠 것인가, 한계로 서술할 것인가** — **원인 규명됨(2026-07-31): M=100 부분표본 학습.** in-distribution 에서도 z_std=3.59 라 분포이동이 아니다. 선택지 (i)M 확대 (ii)홀드아웃으로 sigma_n 추정 (iii)제대로 된 sparse GP=Phase 9 (iv)한계로 서술 | `gp-residual.md` + 위 ★★★ | 미결 (**최우선**) |
| 3 | 논문 주장 범위 — "어디가 불확실한지"까지만 vs "얼마나"까지 | 서술 | 미결 |
| 4 | `state_kf`/모델보정 KF 튜닝 목적함수 = 추정/외란 RMSE 이지 추종오차가 아니다 | `ekf-baseline.md` 공정성 2 | **확정** — 뒤집으려면 사용자 확인 필요 |
| 5 | 잡음+필터 실험을 기본 구성으로 승격할지 | 서술 | **종결** — 필터링이 기본 구성(위 ⚠️⚠️) |
| 6 | Phase 8 수치의 조건부 서술 — "잔차=z의 매끄러운 함수" 전제를 리포트에 명시할지 | `handoff-log-phase8.md`「실차 전이 한계」 | 미결 |
| 7 | dlc(과신 유지) vs rt_rturn(캘리브레이션 개선) — 같은 "형상외삽"인데 왜 반대인가 | `handoff-log-phase8.md`「Phase 8b 확장」 | 미결 |
| 8 | ~~어느 피드백 구성을 주 결과로 삼을지~~ | 위 ⚠️⚠️ | **종결** — 필터링 구성 기본, 생측정은 ablation |
| 9 | **★ 축 B 재측정 범위** — 합성경로 9런·깨끗한 트랙 9런이 옛 목적함수 수치 | 위 ⚠️⚠️ | 미결 (최우선급) |
| 10 | ~~모델보정 `Q_d` 재튜닝~~ | 위 ★★★ | **종결** (2026-07-30) — 원인은 추종오차 기준 튜닝(미결 #4 위반). Q_d x10 으로 교정, held-out 에서 NLPD +8.49→−3.39 |
| 11 | `R_delta` 의 잡음 억제 역할 — 축 B 는 깨끗한 조건 근거로 0 확정했으나 잡음 조건에서는 부수적 억제 효과가 있었을 수 있다(추측) | `handoff-log-phase8.md` | 미결 |
| 12 | loop10 자체를 평가 경로로 쓰는 잡음+필터 시나리오 작성 — `rtf_*` 9개는 여전히 racetrack 을 **평가** 경로로 쓴다(학습 출처는 loop10 으로 이미 분리됨, #13 아님) | — | 미결 |
| 13 | ~~`configs/gp/loop10.yaml` 의 `tuning_selection_scenario` 미확정~~ | — | **종결** (2026-07-31) — `part1_gp_lpf_ay4` 로 채움 |
| 14 | **모델보정 KF 의 채널별 `Q_d` 비율** — 이번 재튜닝은 두 채널을 같은 배수로만 스케일했다. rtf_ay6 에서 gamma 채널만 캘리브레이션이 나쁘다(v_y 1.045 vs gamma 2.299) | 위 ★★★ | 미결 (신규, 2026-07-30) |
| 15 | **★ KF 가 GP 를 캘리브레이션·추종 모두에서 앞선다** — 비교군을 정당하게 강화한 결과. #2·#3 과 묶어 논문 논증 재설계 필요 | 위 ★★★ | 미결 (최우선급). **2026-07-31 갱신**: 분산 수정 + M=2000 으로 GP 가 크게 좋아졌으나(z_std 3.59->2.19) 여전히 KF(0.83)에 진다 |
| 16 | **★ 홀드아웃 z_std 1.241 vs 폐루프 2.192 격차** — 같은 GP 인데 평가 방식만 다르다. 가설(미검증): (a) GP 보정이 방문 상태분포를 바꾸는 자기 유발 분포이동, (b) 지평 상수 주입이라 `z_now` 평가 후 상태가 움직인 만큼이 분산에 미반영 | 위 ★★★ | 미결 (**최우선**, 신규 2026-07-31) |

**#1 상세** — dlc 에서 MPC-only 1.03e-2 < KF 1.20e-2 < GP 1.29e-2(옛 비용함수).
가설: (a) 지평 상수 외란 주입이 좌우 교대 곡률에서 부호가 계속 뒤집혀 해로움
(구조적 한계), (b) dlc 의 vx 프로파일이 더 공격적이라 튜닝이 안 맞음. 판별 전에
튜닝 건드리지 마라. 판별 실험: dlc 오라클(참 1스텝 잔차 상수 주입) 대조.

**#2 상세** — 원인은 8b 에서 규명됨(`handoff-log-phase8.md`「사후분산은
사전분산을 넘을 수 없다」). 선택지: (i) 한계로 정직히 서술, (ii) 비정상 커널/
입력의존 잡음으로 상한 제거, (iii) z 에 v_x 추가(Phase 7 결정 되돌림, 사용자 확인
필요). **금지**: 커버리지 맞추려고 `sigma_n_floor`·분산을 사후 스케일링.

## 참고 경로

- **작업 프롬프트**: `prompts.md`(사용 규칙) + `prompts-phase-early.md`(Phase -1~2)
  + `prompts-phase-mid.md`(Phase 3~5) + `prompts-phase-late.md`(Phase 6~8).
  Phase 9~10 은 착수 시 `prompts.md` 에 추가.
- ~~★ 현재 비용함수 아래 유일한 완결 실험: `results/part1_20260730_144450/`~~ —
  **무효·이동됨**(2026-07-30). racetrack 학습 GP 를 썼던 실험이라
  `results/results_729_deprecated_racetrack_gp/`로 옮겼다. `mpc_only`/`MPC+KF`
  수치는 여전히 유효(위 rtn_* 표 참조)하지만 `MPC+GP` 열은 재측정해야 한다.
- 과거 결과 폴더는 전부 옛 목적함수(대조용): `results/results_729/` 아래
  `part1_20260726_163931`(Phase 8) 등. `_190107`(8c), `_000727`(rt_rturn),
  `_002844`(rtn_rturn) 등 racetrack-GP 를 쓴 것들은
  `results/results_729_deprecated_racetrack_gp/`로 이동됨.
- **GP 학습 config**: `configs/gp/loop10.yaml`(유일한 학습 출처, loop10 10바퀴) +
  `configs/experiment/gp_train_loop10.yaml` + `configs/sim/gp_train_loop10.yaml`.
  폐루프 N바퀴 기능은 `src/config.py`(`PathConfig.closed_loop`,
  `SimConfig.n_laps`) + `src/path/reference.py`(`kappa_of_s` wrap) +
  `src/sim/runner.py`(종료 조건).
- **README.md/RESULTS.md 는 무효 상태다** — 옛 목적함수 수치 (미결 #9 이후 갱신)
- 룰: `.claude/rules/` (모듈 건드리기 전에 해당 파일 먼저 Read)
- 게이트 정의: `.claude/rules/testing.md`
- 부호 규약 단일 기준: `.claude/rules/vehicle-model.md`
- MPC 정식화·솔버 상세: `.claude/rules/mpc-solver.md` + `mpc-solver-notes.md`
- 실험 조합: `configs/experiment/part1_*.yaml`, `part2_*.yaml` (미작성)
