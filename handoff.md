# handoff

세션이 끝나거나 `/clear` 하기 전에 이 파일을 갱신한다. 아래 5개 섹션 구조를 유지하라.

---

## 오늘 한 일 (3줄)

- 2026-07-26/27 — **Phase 0~8 + 8b(외부 트랙, +right_turn 형상외삽) + 8c(현실적 센서)
  완료.** 합성경로 9런 + 레이싱트랙 7런(rt_ay4/rt_ay6/**rt_rturn**) + 잡음 6런.
- **rt_rturn — GP가 추종·캘리브레이션 둘 다 KF를 이긴 첫 시나리오** (−18.0% 추종,
  z_std 1.080). "GP는 항상 과신"이라는 기존 서술은 이제 예외가 있어 수정 필요.
- **8b — GP 과신의 원인 ①**: 사후분산이 사전분산 σ_f² 에 막혀
  (rt_ay6 에서 오차가 상한의 3.19배, 랩의 39% 가 상한 포화) 커버리지 52.6% 붕괴.
- **8c — 원인 ② + 예상 반전**: 센서 잡음 하에서도 **GP 추종 우위 유지**(−13.1%, KF 대비
  격차는 오히려 증가). 단 조향 9배 떨림 → 캘리브레이션 실패의 91%가 **GP가 가장
  자신 있는 구간**에 집중(|오차|/σ=6.14). 신뢰도가 오차와 역상관.
- **핵심 UQ 주장 성립**: GP 사후 std 가 분포 밖에서 **28~30배 증가**
  (2.3e-4 → 6.3e-3/6.9e-3). 게이트 `test_posterior_std_grows_out_of_distribution` 통과.
- **그러나 절대 캘리브레이션 실패**: GP 는 **모든** 시나리오에서 과신
  (z_std 1.57~3.25, 95% 구간이 77~82%만 포함). KF 는 오히려 보수적(89~99%). 아래 상세.

## Phase 8c 결과 (현실적 센서 잡음 — 2026-07-26 추가)

Phase 8까지는 **세 케이스 모두 참 상태 피드백**(이상적 센서)이었다. 여기서 그 가정을
걷어냈다. `results/part1_20260726_190107/`.

**설정**: `configs/sensor/realistic.yaml` — 가우시안 백색잡음
std=[0.03 m/s, 0.002 rad/s, 0.003 rad, 0.03 m] (v_y·γ·e_ψ·e_y). 사인 바이어스는
구현했으나 **기본 0** — 결정론적 바이어스는 KF의 영평균 가정을 깨 비교군을 부당하게
약화시킨다(ekf-baseline.md). 세 케이스가 **같은 잡음 실현**을 본다(센서는 한 벌).
피드백 경로는 세 케이스 모두 생측정값(사용자 확정, Phase 6 통제 철학 유지).

| 시나리오 | MPC only | MPC+KF | MPC+GP |
|---|---|---|---|
| rtn_ay4 (잡음, in-dist) | 1.4453e-2 | 1.3532e-2 (−6.4%) | **1.2559e-2 (−13.1%)** |
| rtn_ay6 (잡음, 강도외삽) | 3.2922e-2 | **2.8934e-2 (−12.1%)** | 2.9186e-2 (−11.4%) |

| 시나리오 | KF z_std / 95%커버 / NLPD | GP z_std / 95%커버 / NLPD |
|---|---|---|
| rtn_ay4 | 9.319 / 38.0% / +37.59 | 9.278 / 53.6% / +37.88 |
| rtn_ay6 | 17.099 / 24.5% / +140.35 | **4.864 / 34.5% / +7.50** |

**결론 셋:**

1. **GP는 잡음 하에서도 in-distribution 추종 우위를 유지한다** (−13.1%). KF 대비 격차가
   오히려 **커졌다**(깨끗: GP−16.0% vs KF−10.3% = 5.7pp / 잡음: −13.1% vs −6.4% = 6.7pp).
   "GP는 잡음에 취약할 것"이라는 사전 예상은 **추종 축에서 기각**됐다.
2. **KF의 외삽 우위는 완벽한 센서의 산물이었다.** 깨끗한 rt_ay6에서 KF는 −32.5%로 GP를
   압도했으나, 잡음이 들어가자 −12.1%로 떨어져 GP(−11.4%)와 동률이 됐다.
3. **캘리브레이션은 양쪽 다 붕괴**하지만 외삽에서는 GP가 명백히 낫다(NLPD +7.5 vs +140).

### ★ 잡음 하 캘리브레이션 붕괴의 메커니즘 (측정으로 규명)

**기각된 가설**: "GP는 입력이 정확하다고 가정하므로(errors-in-variables) 잡음 낀 z에서
평가한 게 원인이다." → **틀렸다.** 참 z로 재평가해도 오차가 동일했다
(0.008612 vs 0.008592). 입력 잡음이 μ에 주는 변동 RMS는 9.5e-4로 사후 std(3.8e-3)보다
작다. 재조사 금지.

**실제 원인** (rtn_ay4 기준):

| 지표 | 깨끗(rt_ay4) | 잡음(rtn_ay4) |
|---|---|---|
| delta 변화율 RMS | 0.088 rad/s | **0.798 rad/s (9배)** |
| z² 총합 중 **사후std 하위25%** 구간 비중 | 0.8% | **91.3%** |
| 그 구간의 평균 \|오차\|/σ | 0.19 | **6.14** |

즉 잡음 → **조향이 9배 떨림** → 플랜트가 GP가 학습한 매끄러운 다양체에서 벗어남 →
그 오차가 **GP가 가장 자신 있는 구간(σ 작은 곳)에 집중**된다. 캘리브레이션 실패의
91%가 거기서 나온다.

**함의**: GP의 epistemic 불확실성은 "데이터에서 얼마나 먼가"만 재고
**"배치 조건이 바뀌었는가"는 재지 못한다.** 잡음 하에서는 신뢰도가 오차와 오히려
역상관이라 경고 기능을 전혀 못 한다. 8b의 사전분산 상한 문제와는 **별개의 두 번째
구조적 한계**다.

### ⚠️ KF 초기 발산 — 내 설정 실수였고 재튜닝으로 해결

첫 실행에서 rtn_ay6의 KF가 **RMS e_y 3454 m로 발산**했다(d_hat이 3929 m/s²까지 폭주,
참 외란 스케일 ~0.65. delta 76% 포화, IPOPT 수렴실패 0% → 솔버가 아닌 폐루프 발산).

원인: `R_kf`만 센서 사양에 맞추고 **`Q_kf`는 이상적 센서 시절 값(Q_d=1.0)을 그대로 뒀다.**
필터 거동을 정하는 건 Q/R **비율**인데 한쪽만 바꾼 것이다. 측정잡음을 외란으로 오인 →
MPC 과격 조향 → 상태 변화 → 더 큰 외란 추정의 양성 피드백.

합의된 공정성 프로토콜대로 재튜닝: d블록 스케일 8점 그리드, **in-distribution(rtn_ay4)만
보고 결정**, 평가 시나리오 열람 금지 → scale 0.1 채택 → 발산 해소(2.89e-2).
과정은 `configs/ekf/realistic_sensor.yaml` 주석에 전부 기록.

**남은 미결(신규)**: Q_d를 **추종 RMS 기준**으로 튜닝했더니 캘리브레이션이 나빠졌다
(rtn_ay4 KF: z_std 3.300→9.319, NLPD +0.45→+37.59). Part 1의 주 지표가 캘리브레이션인데
튜닝 목적함수는 추종이었다는 불일치다. NLPD 기준 재튜닝을 할지 결정 필요.

## Phase 8b 확장 — right_turn 형상외삽 (2026-07-27 추가)

racetrack GP(#2)에게도 dlc 같은 형상외삽 축을 만들었다. 방향 반전은 판단 단계에서
기각(원본 트랙이 이미 좌우 혼합이라 반전해도 새 영역이 아님 — 딕셔너리 69/29% 분포로
확인). 대신 강의 자료(실습 1차 배포본)의 **별도 트랙** `path_data_right_turn.mat`
(512.0m, 76.2% 우회전 전용, 좌회전 0%)을 사용. 착수 전 임시 스크립트로 GP #2 사후분산이
실제로 오르는지 먼저 검증(평균 2~3배, 최대 14.1배 — 반전과 달리 진짜 새 영역 확인 후 채택).

**만든 것**: `configs/path/right_turn.yaml`(sha256 고정, 자기정합 3.44e-4·계단형 없음
검증됨) + `part1_{mpc_only,kf,gp}_rt_rturn.yaml`(a_y_max=4.0 통제, 형상만 rt_ay4와 다름)
+ `run_part1.py` SCENARIOS_RACETRACK 등록. `results/part1_20260727_000727/`.

| 시나리오 | MPC only | MPC+KF | MPC+GP |
|---|---|---|---|
| rt_ay4 (참고) | 9.169e-3 | 8.222e-3 (−10.3%) | 7.705e-3 (−16.0%) |
| **rt_rturn** | 9.172e-3 | 8.247e-3 (−10.1%) | **7.518e-3 (−18.0%)** |

| 시나리오 | KF z_std/95%커버/NLPD | GP z_std/95%커버/NLPD |
|---|---|---|
| rt_ay4 (참고) | 0.601/99.2%/-5.03 | 1.748/85.4%/-6.95 |
| **rt_rturn** | 0.606/99.2%/-5.02 | **1.080/94.1%/-5.67** |

### ★ 지금까지 중 GP한테 가장 좋은 결과 — 추종·캘리브레이션 둘 다 KF를 이김

rt_rturn은 **GP가 자기 학습 트랙(rt_ay4)보다도 추종(−18.0% vs −16.0%)과 캘리브레이션
(z_std 1.080 vs 1.748, 이상값 1.0에 근접) 둘 다 더 좋게 나온 첫 시나리오**다. 주 그림
(`part1_rt_rturn_uncertainty.png`)을 보면 궤적이 GP 딕셔너리 밀집 구간
($v_y{\approx}0{\sim}0.5,\gamma{\approx}-15{\sim}-20$)을 정확히 관통한다 — 별도 검증
스크립트가 측정한 "평균 사후 std 2~3배 증가"는 학습 트랙 자체가 저곡률 구간에 시간을
많이 써 평균이 낮게 깔린 결과이며, 실제 오차 대비 비율은 여전히 잘 맞았다.

**dlc(GP #1)와 정반대**: 같은 "형상외삽"인데 dlc는 KF·GP 둘 다 MPC-only에 짐(+17%,+25%,
z_std 개선되나 여전히 과신 1.572). rt_rturn은 GP가 셋 중 최고. **원인 미확인(추측
수준)** — dlc는 좌우 교대라는 확실한 새 형태인 반면, right_turn은 원본 트랙이 이미
어느 정도 우회전을 포함하고 있어 딕셔너리와 겹쳤을 가능성. 검증 필요 — 미결 #7.

**기존 서술 수정 필요**: 아래 Phase 8 절의 "GP 는 세 시나리오 전부 과신이다"는 그
3-way 표(ay4/ay6/dlc) 한정으로는 여전히 사실이나, **전체 그림에서는 더 이상 "GP는
항상 과신"이라고 뭉뚱그릴 수 없다.** rt_rturn 은 z_std 1.080 으로 사실상 잘
캘리브레이션됐다. 미결 #2(GP 과신 처리)에 이 예외를 반영해야 한다.

### rtn_rturn — 형상외삽 + 잡음 (2026-07-27 추가)

rt_rturn 이 잡음에서도 버티는지 확인. `configs/experiment/part1_*_rtn_rturn.yaml` 3개
+ `run_part1.py` SCENARIOS_NOISY 등록. `results/part1_20260727_002844/`.

| 시나리오 | MPC only | MPC+KF | MPC+GP |
|---|---|---|---|
| rt_rturn (깨끗) | 9.172e-3 | 8.247e-3 (−10.1%) | 7.518e-3 (−18.0%) |
| **rtn_rturn (잡음)** | 1.391e-2 | 1.355e-2 (−2.6%) | **1.279e-2 (−8.1%)** |

| 시나리오 | KF z_std/95%커버/NLPD | GP z_std/95%커버/NLPD |
|---|---|---|
| rt_rturn (깨끗) | 0.606/99.2%/-5.02 | 1.080/94.1%/-5.67 |
| **rtn_rturn (잡음)** | 8.513/39.3%/+30.41 | **3.163/58.6%/+0.29** |

**★ 형상축의 GP 우위는 잡음에서도 안 무너진다** — 강도축(rt_ay6)은 KF의 깨끗한 우위
(−32.5%)가 잡음에서 −12.1%로 붕괴해 GP(−11.4%)와 동률이 됐지만, 형상축은 GP-KF
격차가 깨끗(7.9pp)·잡음(5.5pp) 둘 다 유지된다(순위 안 뒤집힘). 단 캘리브레이션은
양쪽 다 심하게 무너진다(z_std 3~9배) — 절대 안전 지표로는 못 쓴다. GP 쪽이 KF보다는
덜 나쁘다(z_std 3.163 vs 8.513, rtn_ay4의 GP 9.278 보다도 낮음 — 형상축이 강도축보다
잡음 하에서도 상대적으로 낫다는 패턴이 유지).

**조향 변화율 47% 포화 확인 — 새 버그 아님**: mpc_only 조차 47.4% 포화(세 케이스 균일,
초중후반 균일하게 분포 — 특정 구간 문제 아님). rtn_ay4(46.8%)·rtn_ay6(46.6%)와 동일
수준임을 대조 확인. Phase 8c 에서 찾은 "조향 변화율 9배 증가(0.088→0.798 rad/s)"의
실체가 바로 이것 — **액추에이터 속도한계에 거의 절반의 시간 동안 붙어 있다.** 이는
버그가 아니라 "세 케이스 모두 생측정값 피드백"(Phase 8c 확정 결정)의 직접적 대가:
KF 가 있어도 그 필터링 결과가 제어 피드백에는 반영되지 않으므로(d_hat 보정만 사용),
raw 잡음이 매 스텝 그대로 MPC 비용함수를 흔든다. solve time 도 동반 상승(mean
13~17ms, dt초과 최대 6.7%, 이전 깨끗 시나리오는 9ms대·초과 0%대) — 전 잡음 시나리오
공통, Part 2(실시간성) 논의 시 재검토 필요.

## ★ 실차 전이 한계 (sim-to-real gap) — 2026-07-26 논의 정리

Phase 8의 전제("잔차 = 타이어 비선형성 하나")가 실차·LIMO로 갈 때 얼마나 유지되는지
정리한다. 결론부터: **Phase 8 수치는 "잔차가 z의 매끄러운 함수"라는 이상화 조건에서
나온 것**이고, 이 조건이 깨지는 순간(이미 8c에서 한 번 봤다) 캘리브레이션이 먼저 무너진다.

### 문헌 확인 — 다른 연구는 잔차를 한 원인으로 안 좁힌다

이 프로젝트는 오차 기구학을 고정하고 타이어 힘 법칙만 교체해 잔차 출처를 **의도적으로
하나로 통제**했다(CLAUDE.md 실험 설계). 이게 이 분야의 표준인지 확인함:

- Hewing, Kabzan, Zeilinger, *Cautious MPC using Gaussian Process Regression*
  (arXiv:1705.10702) — GP가 배우는 항을 "additive nonlinear part of the dynamics"로만
  칭하고 **물리적 원인을 특정하지 않는다.** 차량 케이스에서 원인 언급 없음(유일한 구체
  사례는 AUV 마찰 효과).
- *GP-based MPC for Overtaking* (Frontiers in Neurorobotics, 2021) — 명목=simplified
  Pacejka, 플랜트=full Magic Formula로 우리와 설정이 비슷하지만, 잔차를 "model mismatch
  and unmodeled dynamics, as well as process noise"로 뭉뚱그려 서술한다.
- Kabzan et al. 레이싱카 논문(arXiv:1711.06586)은 PDF/HTML 변환 실패로 **본문 미확인**.

즉 일반적으로는 잔차를 여러 원인이 섞인 채로 통째로 학습시킨다(실차 데이터는 애초에
분리가 안 된다). 이 프로젝트의 단일원인 통제는 **인과관계를 깨끗하게 검증하기 위한
의도적 선택**이지, 실차 잔차를 대표하는 설정이 아니다. 이 구분을 리포트에 명시할 것.

### 잔차가 늘어나는 두 가지 경우 — 성격이 다르다

| | 정의 | GP가 원리상 대응 가능한가 |
|---|---|---|
| **경우 A** | 잔차가 여전히 z(또는 확장된 z)의 결정론적 함수 (combined slip, 하중이동 등) | 가능 — 데이터·M 늘리면 됨. "더 어려워질 뿐" |
| **경우 B** | 잔차가 z에 없는 숨은 변수·이력에 의존 (타이어 이완 지연, 액추에이터 내부상태, 노면 패치) | **불가능** — 같은 z에 다른 r이 대응 → 데이터로 안 고쳐짐 |

### 경우 B 의 실측 사례 — Phase 8c 가 이미 축소판을 보여줬다

센서 잡음(설계에 없던 교란원 하나 추가)만으로 조향이 9배 떨리고, 그 결과 플랜트가
GP의 매끄러운 다양체를 벗어나 **캘리브레이션 실패의 91.3%가 GP 최고신뢰 구간에 집중**됐다
(위 Phase 8c 결과 참조). 실차에서는 이런 교란원이 한 종류가 아니라 여러 종류(§ 아래)
동시에 들어오므로, 이 효과가 더 심해질 것으로 예상한다(**추측** — 실측 전까지 확정 아님).

### LIMO — 타이어 비선형성 잔차에 도달하기도 전의 문제

1. `configs/vehicle/limo.yaml`의 $C_f,C_r,I_z$가 **전부 미검증 추정치**(YAML TODO).
   이건 비선형성이 아니라 **명목 선형모델 자체가 틀린 값**이라 잔차의 성격이 아예
   다르다("타이어가 포화됨"이 아니라 "선형 영역에서도 상수만큼 어긋남").
2. Phase 5 특성화(위 참조)에서 이미 확인: limo는 `vx_range`가 좁아 크롤 속도(~1 m/s)에
   머물고 슬립각이 거의 0° → **타이어가 비선형 영역에 안 들어간다.** "잔차=타이어
   비선형성" 전제가 limo에는 적용 안 될 가능성이 높다. 실제 지배적 잔차는 모터/휠
   동역학이나 파라미터 오차 쪽일 것(**추측**).

→ LIMO 착수 전 $C_f,C_r,I_z,\delta_{max}$ 식별 실험이 필수인 이유가 여기서도 확인된다
(기존 「절대 하지 말 것」·prompts.md 항목과 일치, 새로 추가된 근거는 위 1·2).

### 이게 실패가 아닌 이유

통제실험(Phase 8) → 교란 추가(8b 형상/강도, 8c 센서잡음) → 실차(예정)로 이어지는 사다리는
원래 이렇게 설계하는 것이다. 8c가 "무너질 수 있다"를 보여준 건 다음에 무엇을 확인해야
하는지 알려준 것이지 실험 설계의 결함이 아니다. 다만 **논문/리포트에는 Phase 8 수치가
"잔차가 z의 매끄러운 함수"라는 조건부 결과임을 명시**해야 한다 — 아래 미결 #6.

## Phase 8b 결과 (외부 레이싱 트랙 — 2026-07-26 추가)

사용자 제공 `path_data_racetrack.mat` (자율주행이론 실습 2차 배포본)로 6런
(3케이스 × rt_ay4/rt_ay6). `results/part1_20260726_170758/`.

**데이터 검증** — `map = [s; x; y; theta; kappa]`, ds=0.5 m 균일, 955.5 m 폐곡선.
곡률이 이미 들어 있어 수치미분 불필요. `|dtheta/ds - kappa|` max 7.5e-4 (자기정합),
`dkappa/ds` max 0.003 1/m² (계단형 없음), κ∈[-0.025,+0.031] R_min=32.4 m.
곡률만으로 복원한 (x,y)가 원본과 956 m 주행에서 max 1.0 m 오차, 최종점 정확히 일치.

| 시나리오 | MPC only | MPC+KF | MPC+GP |
|---|---|---|---|
| rt_ay4 (in-dist) | 9.169e-3 | 8.222e-3 (−10.3%) | **7.705e-3 (−16.0%)** |
| rt_ay6 (강도외삽) | 2.840e-2 | **1.916e-2 (−32.5%)** | 2.470e-2 (−13.0%) |

| 시나리오 | KF z_std / 95%커버 | GP z_std / 95%커버 |
|---|---|---|
| rt_ay4 | 0.601 / 99.2% | 1.748 / 85.4% |
| rt_ay6 | 1.879 / 85.0% | **3.255 / 52.6%** |

- **in-distribution 에서 GP 우위가 합성경로보다 더 크다** (−16.0% vs 합성 −12.2%).
  캘리브레이션도 합성 ay4(3.25/76.7%)보다 낫다(1.75/85.4%). 외부 트랙에서도 논지 성립.
- **외삽에서는 KF 가 GP 를 명백히 이긴다** (−32.5% vs −13.0%). 합성 ay6 에서는 동률이었다.
- **GP 커버리지가 52.6% 로 붕괴.** v_y 채널 z_mean −1.087(큰 계통편향), NLPD 가 양수(+0.25)로
  KF(−3.43)보다 나쁘다.

### ★ 원인 규명 — 사후분산은 사전분산을 넘을 수 없다 (구조적 한계)

| 시나리오 | 평균 사후 std | RMS 오차 | 배율 | 상한 포화 비율 |
|---|---|---|---|---|
| rt_ay4 | 6.11e-4 | 1.69e-3 | 2.77× | 0.0% |
| rt_ay6 | 7.74e-3 | 3.43e-2 | 4.44× | **39.3%** |

사후 std 는 분포 밖에서 **12.7배 증가**(v_y)해 핵심 UQ 주장은 성립한다. 그러나 실제
오차는 **20.3배** 늘어 비율이 2.77→4.44 로 악화된다.

**측정된 원인**: 영평균 정상 GP 의 사후분산은 **사전분산 σ_f² 를 절대 넘을 수 없다.**
이 GP 의 사전 std = **1.076e-2 m/s** (v_y). 그런데
- rt_ay4 RMS 오차 1.69e-3 = 상한의 **0.16배** → 여유 충분, 커버리지 85.4%
- rt_ay6 RMS 오차 3.43e-2 = 상한의 **3.19배** → 표현 불가능, 랩의 39.3% 가 상한에 붙박임

즉 **아무리 데이터에서 멀어져도 GP 는 "이만큼 틀렸다"를 말할 수 없다.** 튜닝 문제가
아니라 커널·평균함수 선택의 구조적 성질이다. 이것이 커버리지 붕괴의 기계적 원인이며,
`sigma_n_floor` 나 분산 스케일링으로 고칠 수 없다(고쳐서도 안 된다).

## Phase 8 결과 (Part 1 실험 — 숫자 원본)

`results/part1_20260726_163931/summary.csv`. sedan, 비선형 플랜트, seed 0, 통제변수
자동검증 통과(`assert_controlled`).

**추종 RMS e_y [m] (괄호 = MPC-only 대비):**

| 시나리오 | MPC only | MPC+KF | MPC+GP |
|---|---|---|---|
| (a) ay4 in-dist | 9.56e-3 | 9.01e-3 (**−5.8%**) | 8.39e-3 (**−12.2%**) |
| (b) ay6 강도외삽 | 2.79e-2 | 1.89e-2 (−32.1%) | 1.90e-2 (−31.9%) |
| (c) dlc 형상외삽 | 1.03e-2 | 1.20e-2 (**+17.0%**) | 1.29e-2 (**+25.1%**) |

- (a)는 Phase 7 수치(−12% / −5.8%)를 **정확히 재현**했다. 순서 GP>KF>only 유지.
- (b)에서 GP 의 우위가 사라진다 (KF 와 사실상 동률).
- **(c)에서 KF·GP 둘 다 MPC-only 보다 나쁘다.** `ekf-baseline.md` 「비교군은 반드시
  단독 MPC 를 이겨야 한다」가 이 시나리오에서 깨진다. 미해결 — 아래 미결 표 #1.

**UQ 캘리브레이션 (pooled, 대상 = 1스텝 이산 잔차):**

| 시나리오 | KF z_std / 95%커버 | GP z_std / 95%커버 |
|---|---|---|
| (a) ay4 | 0.587 / 99.4% | 3.252 / 76.7% |
| (b) ay6 | 1.382 / 89.1% | 3.025 / 76.5% |
| (c) dlc | 0.995 / 92.9% | 1.572 / 81.9% |

`z_std > 1` = 과신(위험), `< 1` = 보수적. **GP 는 세 시나리오 전부 과신이다.**

**GP 평균 사후 std vs 실제 오차 (진단, v_y 채널):**

| 시나리오 | 평균 사후 std | RMS 오차 |r−mu| | 배율 |
|---|---|---|---|
| ay4 | 2.29e-4 | 7.84e-4 | 3.4× |
| ay6 | 6.33e-3 | 2.12e-2 | 3.3× |
| dlc | 6.86e-3 | 1.60e-2 | 2.3× |

→ **std 는 옳은 방향으로 28~30배 커진다(주장 성립). 다만 어느 지점에서도 일관되게
3배가량 작다.** 즉 "어디가 불확실한지"는 맞히지만 "얼마나"는 못 맞힌다.

**기각된 가설 (재조사 금지)**: 결합이 `z=[v_y,gamma,delta_prev]` 를 쓰는데 잔차는
실제 인가된 `delta_k` 로 생기므로 그 불일치가 편향의 원인이라고 의심했으나, `delta_k`
로 다시 평가해도 오차가 동일했다(ay4 에서는 오히려 약간 악화). **원인이 아니다.**

## Phase 5 특성화 결과 (GP 설계 입력 — 숫자를 보고 Phase 6+ 결정)

**sedan mismatch, a_y_max=4.0, single_curve, 잔차 SNR = RMS/noise_floor:**

| 채널 | 잔차 RMS | SNR | 비고 |
|---|---|---|---|
| v_y   | 1.30e-2 | ×1.36M | GP 학습 대상. 강한 신호 |
| gamma | 1.21e-3 | ×1.33M | GP 학습 대상 |
| e_psi | 1.26e-5 | ×68k | = 0.5·dt·r_gamma (종속) |
| e_y   | 1.33e-4 | ×235k | = 0.5·dt·r_vy (종속) |

**a_y_max 스윕 (SNR 단조 증가, 슬립각으로 타이어 작동영역 확인):**

| a_y | SNR(v_y) | SNR(gamma) | max\|α_f\| |
|---|---|---|---|
| 1.0 | ×70k | ×53k | 0.74° |
| 4.0 | ×1.36M | ×1.33M | 3.68° |
| 6.0 | ×3.86M | ×4.87M | 6.85° |
| 7.0 | ×6.56M | ×13.0M | 9.99° |

(alpha_sl≈15.4°. a_y=4~6이 비선형-비포화 영역, GP 실험 후보.)

- **limo**: single_curve에서 1 m/s 크롤 → 20s에 s=20m(커브 진입 못함), 슬립 0°,
  타이어 선형 → GP 신호 없음. limo 전용 경로 + 파라미터 식별 필요(미룸).
- 산점도: `results/mismatch_sedan_*/…_scatter.png` — (v_y,gamma,delta) 커버리지.

## 측정값 (기준선 — 이후 잔차 해석에 사용)

**noise floor** (모델 완전일치, RK4(dt=0.02) vs DOP853, 1-step, sedan vx=15):

| 채널 | RMS | max |
|---|---|---|
| v_y [m/s]    | 9.57e-9  | 1.28e-8 |
| gamma [rad/s]| 9.09e-10 | 1.22e-9 |
| e_psi [rad]  | 1.84e-10 | 2.47e-10 |
| e_y [m]      | 5.67e-10 | 7.90e-10 |

- 진단 임계값 = 위 채널별 값의 10배 (gp-residual.md). Phase 4에서 잔차와 대조.
- 시상수: sedan tau≈0.126s (고유값 -7.93±3.04i, 감쇠진동),
  limo tau≈0.010s (고유값 -119,-100, 과감쇠). limo Iz는 미식별 근사값.
- `gamma_ss(sedan,15,0.02)=0.094675 rad/s` — 적분 대조 통과.

## 다음에 먼저 할 일 — Phase 8 결과 해석 (Phase 9 착수 전)

Phase 8 은 코드·실험 모두 끝났다. 남은 것은 **결과 해석과 그에 따른 설계 결정**이며,
전부 아래 「미결 결정」 표 #1~#3 이다. **이 셋을 정하기 전에 Phase 9(온라인 GP)로
넘어가지 마라** — 과신 문제가 미해결이면 온라인 변형에서도 그대로 따라온다.

논문 서술의 갈림길: 현재 데이터가 지지하는 주장은
**"GP 는 어디가 불확실한지를 안다(상태공간 지도, 28~30배 증가)"** 까지이고,
**"얼마나 불확실한지도 옳게 말한다(캘리브레이션)"** 는 지지하지 않는다.
전자만으로 논문을 쓸지, 후자를 고쳐서 쓸지가 결정 사항이다.

### Phase 8 에서 만든 것

- `src/eval/calibration.py` — 커버리지·NLPD·z 통계. KF 의 연속 외란을 `G P_d G^T` 로
  이산 잔차 공간에 환산(`disturbance_to_residual_ca`). **G/dt 는 단위행렬이 아니다**
  (대각 0.93/0.92, 비대각 −0.125) — `dt·I` 근사는 8~12% 틀린다. 테스트로 고정해 둠.
- `src/sim/assemble.py` — 케이스 조립기. **문자열 분기 없이** 참조된 그룹(gp/ekf)으로
  빌더를 고른다. 새 케이스는 `_CASE_BUILDERS` 한 줄 추가.
- `MpcBase.step_log()` 훅 — runner 가 케이스를 모른 채 `gp_mean`/`gp_var` 를 로깅.
  estimator 훅과 대칭. **Phase 8 이전에는 GP 사후분산이 아예 기록되지 않았다.**
- `ExperimentConfig.plant` / `overrides` — 시나리오를 config 로 표현(스크립트 주입 제거).
- `AugmentedKF.P_d_block` — 대각이 아닌 전체 2×2 (환산에 필요).
- experiment config 9 + `gp_train.yaml`(GP 학습 출처 고정), `configs/viz/case_style.yaml`.
- `scripts/run_sim.py`(케이스 무관) / `scripts/run_part1.py`(3×3 배치 + 통제변수 자동거부).
- `tests/test_part1.py` 14개. 통제변수 검증이 **실제로 불일치를 잡는지**까지 확인.

## Phase 7 산출물 메모

- **결합 = 현재 작동점 상수 주입** (상태의존 지평결합 금지, mean-only 불안정).
  GPStepModel.set_operating_point이 mu_hat=mu_GP(z_now) 1회 평가(CasADi), 지평 상수.
  MpcBase.solve에 작동점 훅 추가(hasattr). M은 solve time 무관, 적합품질만.
- **GP>KF 재정립**: 지평예측 아님. 상태→외란 직접학습이라 즉시 정확(KF는 필터지연).
- 정칙화 sigma_n_floor=1e-2 필수 (없으면 보간 과적합→궤적밖 진동).
- 입력 3D[vy,gamma,delta], 채널2 독립, ARD RBF, Type-II ML(log-det). 직접 numpy GP.
- 분산은 제어 아님, post-hoc 로깅(mean-only). 사후 std 단조증가 확인됨(UQ 논증).
- **주의(Phase 8/분포이동)**: GP는 MPC-only 궤적으로 학습. 상수결합이라 강건하지만,
  더 공격적 시나리오(a_y=6/dlc)에선 외삽 영역↑ → 사후분산↑(UQ 시연 포인트).

## Phase 6 산출물 메모

- **측정 = 전상태 [v_y,gamma,e_psi,e_y]로 확정** (앞선 "부분관측" 뒤집음). 근거 아래.
- **왜 부분관측이 실패했나 (탐구 결론)**: d_vy 는 e_y 로부터 이중적분 뒤에야 관측
  → v_y 미측정이면 심한 지연·과대추정(d_hat 2.0 vs 참 0.65~1.3) → MPC+KF −18%.
  판별실험: 오라클(참 순간외란, 상수외삽) MPC = **+7.3%**(개념 정상). H3(참상태
  잔차 LPF) +9.2%. 부분관측 x_hat 피드백은 발산(v_y lag). → 전상태 측정 + Q_d↑
  = **+5.8%**(오라클 근접). max|e_y|는 −16%(전이 스파이크, Phase 8 튜닝).
- **논문 논리 유지**: KF 가 MPC 를 도움(+5.8%). GP 는 외란을 지평에서 d(x)로 예측해
  상수외삽 상한(+7.3%)을 넘을 것 → GP > KF > MPC-only.
- **StepModel 재확인**: KF(연속 d_hat, RK4 안) / GP(이산 mean, RK4 밖) 둘 다 수용.
- **전방호환**: ekf.py는 평균함수+야코비안 주입. 실차 비선형측정 시 process/meas
  함수만 교체(ca.jacobian 자동). KF≡EKF 테스트로 현재 선형 확인.
- **Part 8 주그림 데이터**: runner가 (ekf_x_hat, ekf_P_diag, ekf_d_hat, ekf_innovation)
  궤적 로깅. GP 사후 std vs KF P d-블록 비교 준비됨.
- **공정 튜닝은 Phase 8**: 현재 Q/R은 MPC+KF 가 이기는 정상값이나 GP 와 같은 예산의
  최종 튜닝은 Phase 8.

## 결정 기록 (Phase 6 확정 / Phase 7 미결)

**확정된 Phase 6 결정 (미결 표 #1·#2에서 이동):**
- EKF 증강상태 = [v_y,gamma,e_psi,e_y,d_vy,d_gamma], 외란은 v_y·gamma에 (Phase 5 근거).
- 측정 = **전상태(v_y,gamma,e_psi,e_y)+잡음** (Phase 6 진단으로 부분관측에서 수정 —
  부분관측은 d_vy 관측지연으로 MPC+KF를 악화시킴). 측정 모델 선형.
- 비교군 = 증강 KF 단독. Part 1 = MPC only / MPC+KF / MPC+GP 3-way.
- **엄밀히 EKF 아니라 LTV KF** (명목 선형). 정직히 명명. 실차 비선형측정 대비
  전방호환(평균함수+야코비안 주입, ca.jacobian 자동). KF→EKF 변환은 필터코어
  불변 + 네 함수 교체만 → 쉬움(확인됨).
- 제어통합: 세 케이스 모두 완전상태 피드백(통제), 차이는 모델보정만. KF는 d_hat
  주입(연속 외란, RK4 안), GP는 mean 주입(이산, RK4 밖).

**확정된 Phase 7 결정 (2026-07-25, prompts.md「Phase 7」에 기록):**
- 입력 z = **[v_y, gamma, delta] (3D)**. v_x 제외 — 슬립각 민감도
  d(alpha_f)/d(vx)~5e-4 rad/(m/s)라 v_x 독립기여 0.04°(무시). 차원 최소.
- 채널 = v_y, gamma 독립 GP 2개. 커널 ARD RBF, Type-II ML(log-det 포함).
- 프레임워크 = **직접 numpy/scipy exact GP** (torch 미사용).
- 결합 = 이산 x_{k+1}=rk4(f_nom)+B_d@mu_GP(z), RK4 밖. mu_GP는 지평 예측상태에서
  평가 → GP가 d(x)를 지평에서 예측(KF 상수 d_hat/오라클 상한 +7.3% 넘음).
- 학습=single_curve a_y=4, 평가=a_y=6 & dlc(분포밖 사후분산 시연). M은 config.
- 게이트: 게이트7 + 사후 std 단조증가 + numpy↔CasADi 일치 + MPC+GP<MPC+KF<only.

**Phase 8 미결:** 아래 「미결 결정」 표 #1~#3 참조. (M 최종값은 Phase 7 상수결합으로
solve time 과 무관해져 미결에서 제외 — 적합 품질 기준으로만 고르면 된다.)

## Phase 5 산출물 메모

- 잔차 계산은 항상 **명목** 대비. 비선형은 plant_rhs 주입만 (runner 무수정).
- Fiala: z=tan(clip(α,±α_sl)) 단일 큐빅 → 경계밖 자동 포화·C1연속, if_else 불필요.
- **기구학 채널 진단**: 매칭은 10×floor 규칙, 불일치는 0.5·dt 비례관계 이탈로 판정
  (gp-residual.md 정정 반영). 절대크기로 버그 판정 금지.

## Phase 4 산출물 메모

- **solve time 정정**: 폐루프 warm start 로 mean 9.2ms (<20ms), dt초과 0%.
  Phase 3의 23ms 우려는 cold+짧은런 편향이었음. 명목 MPC는 실시간 가능.
- 잔차 계산 = `build_step_function(NominalStepModel)` → MPC 예측과 동일 이산 전이
  재사용(복사 아님). 잔차는 항상 **명목** 대비 (GP 케이스도 동일).
- **limo 주의**: single_curve 속도프로파일(a_y=4.0→vx 14)이 sedan용. limo는
  vx≤1.0 clip 크롤(25s에 25m). 절대잔차는 noise-floor 수준이나 sedan floor 대비
  배율(gamma 548×)은 limo의 다른 동역학 스케일 탓, 버그 아님. limo 전용 경로는
  파라미터 식별 후로 미룸.
- StepModel 주입 완료 → Phase 7 GP는 MpcBase/mpc_nominal 수정 없이 붙는다.
  GP StepModel: extra_param_dim>0, step_sym=rk4+B_d@mu_GP, P 확장블록에 Z/alpha.

## Phase 2 산출물 메모

- 세그먼트 스키마 = {length, kappa_end}. 시작곡률=이전끝(0에서 출발) → C0 연속 자동.
- dlc 최대 횡변위 8.5m (실차선보다 큼) — Phase 5에서 잔차 SNR 보고 재조정 예정.
- `Reference(path_cfg, vx_range)` — vx_range는 차량 config에서 주입 (경로 속성 아님).

## 절대 하지 말 것

- 게이트 1 실패 상태에서 MPC 구현 착수.
- GP 보정을 연속 우변(`xdot`)에 더하기 — `dt_ctrl`배 틀린다. 이산 결합만.
- 4x4 전체 `A`에 Hurwitz 검사 걸기 — 원점 고유값 2개는 정상이다.
- 테스트 허용오차를 늘려서 게이트 통과시키기.
- `src/`의 스텁 파일 삭제 (룰 자동 로드 트리거용).
- `results/`, `data/` 커밋.
- **커버리지를 맞추려고 사후분산을 스케일링하거나 `sigma_n_floor` 를 만지기**
  (Phase 8 금지 항목). 과신은 측정된 사실이고, 숫자를 맞추면 측정이 아니라 분식이다.
- **GP 평가가 나쁘다고 `gp_train.yaml` 의 경로·a_y 를 바꾸기** (학습 데이터 몰래 늘리기).
- dlc 의 KF·GP 열세를 튜닝으로 덮기 — 원인 판별(오라클 대조)이 먼저다.

## 미결 결정 (룰 파일에 "(제안)"으로 표시된 항목)

확정되면 해당 룰 파일에서 "(제안)" 표기를 지우고 여기서도 지운다.

**Phase 8 결과가 만들어낸 결정 사항 (Phase 9 착수 전에 정할 것):**

| # | 항목 | 위치 | 상태 |
|---|---|---|---|
| 1 | **dlc 에서 KF·GP 가 MPC-only 보다 나쁘다** — 설계 오류인가 시나리오 특성인가 | `ekf-baseline.md` 공정성 5 | 미결 |
| 2 | **GP 과신을 고칠 것인가, 한계로 서술할 것인가** (원인 2개: 사전분산 상한 + 배치조건 변화 무감지) | `gp-residual.md` | 미결 |
| 3 | 논문 주장 범위 — "어디가 불확실한지"까지만 vs "얼마나"까지 | 서술 | 미결 |
| 4 | **튜닝 목적함수 불일치** — Q_d 를 추종 RMS 로 튜닝하니 캘리브레이션이 나빠졌다(z_std 3.3→9.3). 주 지표가 캘리브레이션인데 목적함수는 추종이었다. NLPD 기준 재튜닝할지 | `ekf-baseline.md` 공정성 2 | 미결 (Phase 8c) |
| 5 | **잡음 실험을 기본 구성으로 승격할지** — 이상적 센서 결과(Phase 8/8b)와 병기할지, 대체할지 | 서술 | 미결 (Phase 8c) |
| 6 | **Phase 8 수치의 조건부 서술** — "잔차=z의 매끄러운 함수" 전제 하 결과임을 리포트에 명시할지, 어느 수준으로 | 위 「★ 실차 전이 한계」 | 미결 |
| 7 | **dlc(과신 유지) vs rt_rturn(캘리브레이션 개선) — 같은 "형상외삽"인데 왜 반대인가** | 위 「Phase 8b 확장」 | 미결 (신규) |

#1 상세 — dlc 에서 MPC-only 1.03e-2 < KF 1.20e-2 < GP 1.29e-2. `ekf-baseline.md` 의
「비교군은 반드시 단독 MPC 를 이겨야 한다」 자동 게이트는 현재 in-distribution 에만
걸려 있어 통과했다. 가설 두 갈래 — **(a)** 지평 상수 외란 주입이 좌우 교대 곡률에서
부호가 계속 뒤집혀 오히려 해로움(구조적 한계, 정직히 보고할 사항), **(b)** dlc 의 vx
프로파일·전이가 더 공격적이라 튜닝이 안 맞음(튜닝 문제). **(a)/(b) 판별 전에 튜닝을
건드리지 마라.** 판별 실험 제안: dlc 에서 오라클(참 1스텝 잔차 상수 주입)을 돌려
그것도 지면 (a), 이기면 (b).

#2 상세 — **원인은 8b 에서 규명됨**(위 「사후분산은 사전분산을 넘을 수 없다」 참조):
사후분산의 상한이 σ_f² 이라 외삽 오차가 σ_f 를 넘으면 표현 자체가 불가능하다.
따라서 선택지는 "고칠 것인가"가 아니라 **"어느 방식으로 상한을 없앨 것인가"** 다:
(i) 한계로 정직히 서술(가장 안전, 현재 데이터가 이미 정량 근거를 준다),
(ii) 비정상(non-stationary) 커널 또는 입력의존 잡음으로 상한 제거,
(iii) z 에 v_x 추가(Phase 7 결정 되돌림 — 사용자 확인 필요. 단 상한 문제 자체는 남는다).
- **금지**: 커버리지를 맞추려고 `sigma_n_floor` 나 분산을 사후 스케일링하는 것
  (Phase 8 프롬프트 금지 항목. 그건 측정이 아니라 분식이다).
- 정당한 선택지: (i) 한계로 정직히 서술, (ii) z 에 v_x 를 넣어 재검(Phase 7 결정을
  되돌리는 것이므로 사용자 확인 필요), (iii) 잔차의 이분산성을 모델링(입력 의존 잡음).

**해소된 미결 (Phase 8 착수 시 확정. 이력 — 되살리지 마라):**

| # | 항목 | 결정 |
|---|---|---|
| 신1 | KF 예측구간 정의 | **d-블록을 이산 잔차로 환산**(`G P_d G^T`, G=ca.jacobian). GP 와 대상 통일. innovation(S)은 부수 자기일관성 지표로만 |
| 신2 | KF/GP 공정 튜닝 예산 | **동일 폐루프 평가 횟수**(각 20회), in-distribution 만 보고 결정, (b)(c) 열람 후 수정 금지. `configs/{ekf,gp}/default.yaml` 에 같은 형식으로 기록 |
| 신3 | 시나리오 config 표현 | **experiment 에 `overrides` 블록**(+ `plant` 키). path 그룹 복제 안 함 |
| 신4 | `sigma_n_floor` 보수성 우려 | **기각** — 실제로는 반대(과신)로 나왔다. 이 걱정은 근거 없었음 |

**해소된 미결 (이력 — 되살리지 마라):**

| # | 항목 | 해소 |
|---|---|---|
| 구1 | EKF 비교군을 외란증강 상태 EKF로 할 것인가 | **확정(Phase 6)** — 증강 LTV KF. 위 「확정된 Phase 6 결정」 |
| 구2 | `P` 대각을 매 스텝 로깅 | **구현됨(Phase 6)** — `runner.py` 가 `ekf_P_diag` 로깅 |
| 구3 | `M` 고정 — solve time 이득 프로파일링 대기 | **무효(Phase 7)** — 상수결합으로 GP 평가가 solve당 1회가 되어 `M` 은 solve time 과 **무관**해졌다. `M` 은 이제 적합 품질만 결정하는 값이라 프로파일링 대상이 아니다. Part 2 sparse(Phase 9)에서는 다른 이유로 다시 다룬다 |

확정된 결정 (되돌리려면 사용자 확인 필요):

- **GP 결합 = 이산 형태 + 지평 상수.** `x_{k+1} = rk4(f_nom) + B_d @ mu_hat`,
  `mu_hat = mu_GP(z_now)`. GP 평가는 **solve당 1회**(작동점). ~~solve당 N회~~ 는
  상태의존 결합 전제의 옛 값 — Phase 7 진단으로 폐기됐다 (`mpc-solver.md` 「GP 결합 시」).
- **온라인 GP 하이퍼파라미터는 고정.** `Z`, `alpha`만 갱신. 재학습은 config 옵트인.
- **진단 채널 임계값 = `noise_floor`의 10배.**

## 참고 경로

- **작업 프롬프트: `prompts.md`** (Phase 0~8 작성 완료. Phase 9~10 은 착수 시 작성)
- **Phase 8 결과: `results/part1_20260726_163931/`** — `summary.csv`, 시나리오별
  `part1_<s>_cases.png`(케이스 비교), `part1_<s>_uncertainty.png`(**주 그림**:
  GP 상태공간 지도 vs KF 시간축), 케이스별 `log.npz` + `meta.json`
- 룰: `.claude/rules/` (7개 — 모듈 건드리기 전에 해당 파일 먼저 Read)
- 게이트 정의: `.claude/rules/testing.md`
- 부호 규약 단일 기준: `.claude/rules/vehicle-model.md`
- 실험 조합: `configs/experiment/part1_*.yaml`, `part2_*.yaml` (미작성)
