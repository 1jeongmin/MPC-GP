# Part 1 / 케이스 4 = racetrack + a_y 증가 (형상+강도 외삽) / GP 잔차 보정 — **입력 워핑**(Var[f*] 확대).
#
# 기준선 `part1_gp_rtf_ay6.yaml`(gp: loop10)과 **gp 그룹 하나만** 다르다. 나머지는 전부
# 동일 — 워핑의 효과만 분리하기 위한 통제(sim-experiment.md 「통제해야 할 변수」).
# 근거·한계는 `configs/gp/loop10_warp.yaml`.
vehicle: sedan
sim: racetrack
path: racetrack
mpc: default
gp: loop10_warp
plant: nonlinear
sensor: realistic
state_kf: default
overrides:
  path.a_y_max: 6.0
